import React from 'react';
import { getLayout } from '~/components/Layout';

const Notification = () => {
	return (
		<div>
			<h1 className="main-title-page">Notification page</h1>
		</div>
	);
};

Notification.getLayout = getLayout;

export default Notification;
